# Upstream-style lookup: only guaranteed-non-existent keys (hit rate cap 0).
BENCH_L2_EXTRA_ARGS="--num-keys 32 --in-flight 1 --data-size-kb 256 --rounds 1 --warmup-rounds 1 --lookup-max-hit-rate 0.0"
