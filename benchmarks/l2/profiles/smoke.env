# Smoke: store -> lookup -> load on the same key idx range (compare / regression).
# Upstream default --lookup-max-hit-rate 0.0 only exercises guaranteed misses.
BENCH_L2_EXTRA_ARGS="--num-keys 32 --in-flight 1 --data-size-kb 256 --rounds 1 --warmup-rounds 1 --lookup-max-hit-rate 1.0"
